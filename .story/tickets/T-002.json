{
  "blockedBy": [
    "T-001"
  ],
  "completedDate": null,
  "createdDate": "2026-05-16",
  "description": "Implement the main daemon entry point: parse CLI args, load config, start the polling loop, and handle graceful shutdown on SIGTERM/SIGINT. The process should exit cleanly, waiting for any active runs to finish before exiting. Distinct from config loading (T-003) — this owns process lifecycle only.",
  "id": "T-002",
  "order": 20,
  "phase": "p0",
  "status": "open",
  "title": "CLI entry point and daemon process lifecycle",
  "type": "task"
}
